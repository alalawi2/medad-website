{
  "name": "medad-website",
  "version": "2.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r *.html *.css *.js *.jpg *.png *.xml *.txt dist/ 2>/dev/null; cp -r images logos dist/ 2>/dev/null; true"
  }
}
